import sys, pygame
pygame.init()

size = width, height = 1280, 720

directions = {"up": True,
              "down": False,
              "left": True,
              "right": False}

screen = pygame.display.set_mode(size)

img = pygame.image.load("img.png")
imgrect 
